from typing import List
import random

def split_training_data(full_data: List, training_fraction: float, validation_fraction: float, test_fraction: float):
    # Split the data into training and validation sets


